-- SELECT traincategory_name, COUNT(traincategory_name) AS number FROM trains_res GROUP BY traincategory_name;

-- SELECT operatorname, COUNT(operatorname) AS number FROM operator_res GROUP BY operatorname;

-- SELECT type, COUNT(type) AS number FROM station_res GROUP BY type;


